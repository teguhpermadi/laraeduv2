use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$leger_full_semester = $teacherSubject->leger->where('category', CategoryLegerEnum::FULL_SEMESTER->value);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$leger_full_semester = $teacherSubject->leger->where('category', CategoryLegerEnum::FULL_SEMESTER->value);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$leger_full_semester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::FULL_SEMESTER->value);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$leger_full_semester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::FULL_SEMESTER->value)->first();
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$leger_full_semester = $teacherSubject->competencyQuran;
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$leger_full_semester = $teacherSubject->competency;
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$leger_full_semester = $teacherSubject->competency->where('half_semester', 1);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$leger_full_semester = $teacherSubject->competency->where('half_semester', 1);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$leger_full_semester = $teacherSubject->competency->where('half_semester', 1)->count();
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$leger_full_semester = $teacherSubject->competency->where('half_semester', 1);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::FULL_SEMESTER->value);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::FULL_SEMESTER->value);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::FULL_SEMESTER->value)->first();
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::FULL_SEMESTER->value);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->with('student');
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->with('student')->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value);
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value);

$legerFullSemester->first()
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value);

$students = $legerFullSemester->first();

foreach($students->metadata as $metadata){
	$metadata;
}
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerFullSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value);

$students = $legerFullSemester->first();

$students->metadata
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerHalfSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value);

$students = $legerHalfSemester->first();

$students->metadata
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerHalfSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value);

$students = $legerHalfSemester->first();

$students->metadata
use App\Enums\CategoryLegerEnum;
use App\Models\Leger;

$teacherSubject = TeacherSubject::find(300);
        
$legerRecapHalfSemester = $teacherSubject->legerRecap->where('category', CategoryLegerEnum::HALF_SEMESTER->value)->first();

$legerHalfSemester = $teacherSubject->leger->where('category', CategoryLegerEnum::HALF_SEMESTER->value);

$students = $legerHalfSemester->first();


