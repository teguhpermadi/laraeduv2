
$timestamp = now()->format('YmdHis');


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif'

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');
$parameter = 'haniif';

$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

$timestamp = now()->format('YmdHis');

$parameter = 'haniif';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);

now();

$timestamp = now()->format('YmdHis');

$parameter = 'haniif';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);




now();

$timestamp = now()->format('YmdHis');

$parameter = 'haniif';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);




$timestamp;



$timestamp = "20250507110445";

$parameter = 'haniif';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);





$timestamp = "20250507110445";

$parameter = 'haniif';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);





$timestamp = "20250507110445";

$parameter = 'haniif';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);





$timestamp = "20250507110445";

$parameter = 'haniif';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);





$timestamp = "20250507110445";

$parameter = 'haniif';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);





$timestamp = "20250507110445";

$parameter = 'haniif';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);





$timestamp = "20250507110445";

$parameter = 'muhammad haniif akhtar rosikh';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);





$timestamp = "20250507110445";

$parameter = 'muhammad haniif akhtar rosikh';


$base = $timestamp . $parameter;


$hash = base_convert(md5($base), 16, 32);


return substr(strtoupper($hash), 0, 26);


$input = 'tegu';
Str::uuidFromBytes(hash('sha256', $input, true))->toString();
use Illuminate\Support\Str;
$input = 'tegu';
Str::uuidFromBytes(hash('sha256', $input, true))->toString();
use Illuminate\Support\Str;
$input = 'tegu';

md5($input);
use Illuminate\Support\Str;
$input = 'tegu';

md5($input);
use Illuminate\Support\Str;
$input = 'tegu';

md5($input);
use Illuminate\Support\Str;
$input = 'tegu';

md5($input);
use Illuminate\Support\Str;
$input = 'tegu';

md5($input);
use Illuminate\Support\Str;
$input = 'tegu';

md5($input);
use Illuminate\Support\Str;
$input = 'tegu' . '07052024';

md5($input);
use Illuminate\Support\Str;
$input = 'tegu' . '07052024';

md5($input);
use Illuminate\Support\Str;
$input = 'tegu' . '07052024';

md5($input);
Student::first()
