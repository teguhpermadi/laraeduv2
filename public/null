use App\Models\StudentInclusive;

StudentInclusive::get();
use App\Models\StudentInclusive;


Grade::with('studentGrade')->get();
use App\Models\StudentInclusive;


Grade::with([
  'studentGrade' => function($q){
  	$q->withoutGlobalScope(AcademicYearScope::class);
  }
])->get();
use App\Models\StudentInclusive;


Grade::with([
  'studentGrade' => function($q){
  	
  }
])->get();
use App\Models\StudentInclusive;


Grade::with([
  'studentGrade' => function($q){
  	$q->withoutGlobalScope(AcademicYearScope::class);
  }
])->get();
use App\Models\StudentInclusive;


Grade::with([
  'studentGrade' => function($q){
  	$q->withoutGlobalScope(AcademicYearScope::class);
  }
])->get();
use App\Models\StudentInclusive;


Grade::with([
  'studentGrade' => function($q){
  	$q->withoutGlobalScope(AcademicYearScope::class);
  }
])->get();
use App\Models\StudentInclusive;


StudentGrade::get();
use App\Models\StudentInclusive;


StudentGrade::withoutGlobalScope()->get();
use App\Models\StudentInclusive;


StudentGrade::withoutGlobalScope(AcademicYearScope::class)->get();
use App\Models\StudentInclusive;


Grade::with(['studentGrade' => function ($query) {
            $query->withoutGlobalScope(AcademicYearScope::class);
         }]);
use App\Models\StudentInclusive;


Grade::with(['studentGrade' => function ($query) {
            $query->withoutGlobalScope(AcademicYearScope::class);
         }])->get();
use App\Models\StudentInclusive;


Grade::with(['studentGrade' => function ($query) {
            $query->withoutGlobalScope(AcademicYearScope::class);
         }])->get();
use App\Models\StudentInclusive;


Grade::with(['studentGrade' => function ($query) {
            $query->withoutGlobalScope(AcademicYearScope::class);
         }])->get();
use App\Models\StudentInclusive;


Grade::with(['studentGrade' => function ($query) {
            $query->withoutGlobalScope(AcademicYearScope::class);
         }])->get();
use App\Models\StudentInclusive;


Grade::with(['studentGrade'])->get();
use App\Models\StudentInclusive;


Grade::with(['studentGrade.student'])->get();
use App\Models\StudentInclusive;


Grade::with(['studentGrade', 'studentInclusive'])->get();
use App\Models\StudentInclusive;


Grade::with(['studentGrade.studentInclusive'])->get();
use App\Models\StudentInclusive;


Grade::with(['studentGrade.studentInclusive'])->get();
