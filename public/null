User::find(1);
use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;

$lastActivityCarbon = Carbon::createFromTimestamp($lastActivity);
use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;

$lastActivityCarbon = Carbon::createFromTimestamp($lastActivity);


$selisihMenit = now()->diffInMinutes($lastActivityCarbon);
use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;

$lastActivityCarbon = Carbon::createFromTimestamp($lastActivity);


$selisihMenit = now()->diffInMinutes($lastActivityCarbon);
use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;





use Carbon\Carbon;

$lastActivity = User::find(1);





use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;





use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;





use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;





use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;





use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;





use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;





use Carbon\Carbon;

$lastActivity = User::find(1)->last_activity;





