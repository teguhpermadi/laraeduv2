TeacherSubject::get()'?>
TeacherSubject::get();
TeacherSubject::first();
TeacherSubject::first()->studentGrade;
TeacherSubjectNote::get();
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
use App\Models\TeacherSubjectNote;

Student::find(1);
use App\Models\TeacherSubjectNote;

Student::with('leger')->find(1);
use App\Models\TeacherSubjectNote;

Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();

use App\Models\TeacherSubjectNote;

Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();

use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();

use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(2);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(1);
use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();

use App\Models\TeacherSubjectNote;
TeacherSubjectNote::get();
Student::with('leger.teacherSubjectNote')->find(3);
use App\Models\LegerNote;
LegerNote::get();
use App\Models\LegerNote;
LegerNote::get();
use App\Models\LegerNote;
Leger::get();
use App\Models\Leger;
Leger::get();
use App\Models\Leger;
Leger::with('note')->get();
use App\Models\Leger;
Leger::with('note')->get();
use App\Models\Leger;
Leger::with('note')->where('teacher_subject_id', 320);
use App\Models\Leger;
Leger::with('note')->where('teacher_subject_id', 320)->get();
